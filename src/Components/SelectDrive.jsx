// import React from "react";
// import Select from "react-select";
// import {OptionsPath} from "../data/types";

// export default function Mode(props) {
//   const handleChange = (e) => {
//     // e.forEach((element) => {
//     //   console.log("in select",element.value);
//     // props.setDetails({type: `${element.value}`})
//     // });
//     props.setDetails({...props.qdata,mode: `${e.values}`})
//   };

//   return (
//     <Select
//       options={OptionsPath}
//       onChange={handleChange}
//       placeholder="what is your mode?"
//     />
//   );
// }